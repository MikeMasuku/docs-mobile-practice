name: CI

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repo sanity check
        run: |
          test -f README.md
          test -f CONTRIBUTING.md
          echo "OK"
